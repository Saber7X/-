#include<bits/stdc++.h>
using namespace std;
const int N = 1e4 + 10;
int a[N];
int b[N];

int main()
{
	int t;
	cin >> t;
	while (t -- )
	{
		int A, B, n;
		cin >> A >> B >> n;
		if (n - a > a)
		{
			cout << -1 << endl;
			continue;
		}
		for (int i = 1; i <= A; i ++ ) cin >> a[i];
		for (int i = 1; i <= B; i ++ ) cin >> b[i];
		
		sort
		
	}
	return 0;
}
